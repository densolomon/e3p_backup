[gcode_macro START_PRINT]
description: "Adaptive bed mesh + Heated bed stabilization + nozzle preheat + safe prime line"
gcode:
    {% set BED_TEMP = params.BED|default(75)|float %}
    {% set HOTEND_TEMP = params.EXTRUDER|default(240)|float %}
    {% set NOZZLE_PREHEAT = 170 %}

    M117 🔥 Старт підготовки...

    # -----------------------------
    # 🔥 Попередній нагрів
    # -----------------------------
    M117 ♨️ Нагрів стола...
    M190 S{BED_TEMP}                    ; heat bed fully with waiting

    M117 ♨️ Попередній нагрів сопла до {NOZZLE_PREHEAT}°C
    M104 S{NOZZLE_PREHEAT}              ; preheat nozzle without waiting

    # -----------------------------
    # 🧭 ADAPTIVE MESH з OrcaSlicer
    # -----------------------------
    M117 📡 Побудова адаптивного мешу...
    BED_MESH_CLEAR
    BED_MESH_CALIBRATE ADAPTIVE=1

    # Очікування охолодження/стабілізації
    M117 ⏳ Стабілізація температури стола...
    G4 P30000   ; 30 секунд (можеш змінити)

    # -----------------------------
    # 🔥 Догрів сопла до робочої
    # -----------------------------
    M117 ♨️ Нагрів сопла до {HOTEND_TEMP}°C
    M109 S{HOTEND_TEMP}

    # -----------------------------
    # 🚀 Позиціонування в межах адаптивного мешу
    # -----------------------------
    {% set px = printer.print_area.min_x + 5 %}
    {% set py = printer.print_area.min_y + 5 %}

    M117 🚀 Перехід до адаптивної зони друку
    G1 Z5 F3000
    G1 X{px} Y{py} F6000

    # -----------------------------
    # 🧵 Прайм-лінія в зоні адаптивного мешу
    # -----------------------------
    M117 🧵 Prime line...
    G1 Z0.25 F3000
    G1 X{px + 120} E30 F600

    # -----------------------------
    # ✔️ Готово до друку
    # -----------------------------
    M117 🚀 Початок друку!
